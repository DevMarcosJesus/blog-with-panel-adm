if (6 am ){
    setTimeout(() => { 6:20 }, 300000);
    acordar, pegar o celular, pressionar o botão de ligar por 1 sec, conferir a hora.

    if(6:20){
        levantar, caminhar até o banheiro,

        if portaQuarto = fechada

        então pegar a chave enfiar na fechadura, girar para a esquerda até parar, 
        então abra a porta, siga caminhando até o banheiro.

        if portaBanheiro = fechada

        então colocar a mão na fechadurada e movimentar para a direita e empurar a portaBanheiro para trás,
        caminhar até o lavatório, pegar escova, se escova = null, então cremeDental = Colgate, e escova = cremeDental,
        escovar os dentes.
    
    }
}else{
    continuar dormindo
}
]